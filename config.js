export default {
  fps: 60,
  walkSpeed: 4, // Pixels per seconds
  zoom: 4,
  tilesetEventCode: { // Tileset configuration events code
    collision: 1,
  },
  inputs: {
    // controles padrão
    up: "ArrowUp",
    down: "ArrowDown",
    left: "ArrowLeft",
    right: "ArrowRight",
  },
}